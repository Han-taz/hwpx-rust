// Field control conversion to HTML — all functions removed (dead code).
// convert_field_ctrl_to_html and its helpers were never called from production code.
// Re-implement when HTML hyperlink rendering is needed.
